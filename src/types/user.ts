export type User = {
  email: string;
  password: string;
};

export type UserInfoApiData = {
  user_id: string;
  username: string;
  expire_time: string;
};

export type UserInfo = {
  name: string;
};
